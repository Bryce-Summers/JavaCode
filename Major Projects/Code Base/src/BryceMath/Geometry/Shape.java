package BryceMath.Geometry;

/* The Superclass for Bryce Shapes.
 * 
 * Used to unify the characteristics of Shapes.
 * 
 * Written on 4 - 24 - 2014.
 * 
 * Each Shape specifies a Set of points in R^2.
 */


public interface Shape
{
	/*
	// Returns the largest shape of type T that is within the intersection of this shape and the input shape.
	public Shape intersection(Shape input);
	
	// Returns the smallest shape of type T that is within the union of this shape and the input shape.
	public Shape union(Shape input);
	*/
	
}
